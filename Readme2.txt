This script fits HMSC models to the phenology data (S1_fit_models) and extracts ther results (S2_extract_results).
The script assumes that the current folder includes:
-the R-file "data" (included in release) under the subfolder "data"
-the subfolder "models" where the fitted hmsc-models will be placed